# Max_AP_MPU

Uses the MPU6050 gyroscope with Max MSP in Wi-Fi access point mode.

## Required Credentials

**`arduino_secrets.h`** — defines Wi-Fi network credentials:

```c
#define SECRET_SSID "your-network-name"
#define SECRET_PASS "your-network-password"
```
