# Wattson

Uses a MPU6050 gyroscope with Max communication over Wifi

## Required Credentials

**`arduino_secrets.h`** — defines Wi-Fi network credentials:

```c
#define SECRET_SSID "your-ssid"
#define SECRET_PASS "your-pass"
```
